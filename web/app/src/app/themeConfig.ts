import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: '#ef4444',
  },
  components: {
    Layout: {
        colorBgHeader: "#f5f5f5",
    }
  }
};

export default theme;
